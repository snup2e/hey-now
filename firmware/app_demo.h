// Path 1 demo entry point.
//
// CubeMX owns main.c; add `app_demo_run();` inside its while(1) loop
// (or call it once — it has its own infinite loop). See INTEGRATION_GUIDE.
#ifndef APP_DEMO_H
#define APP_DEMO_H

void app_demo_run(void);

#endif  // APP_DEMO_H
